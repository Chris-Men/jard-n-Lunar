import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'jardin.lunar',
  appName: 'jardin-lunar',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
